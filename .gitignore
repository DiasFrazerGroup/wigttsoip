.claude
scratch/*
support/private/*
# vendored (own git repo, editable-installed via `pip install -e ./src/alphagenome`)
src/alphagenome
.DS_Store
*ipynb_checkpoints*
.snakemake/
.done
*.pyc
*snakejob*
*snakepipe*
*.private*
*.egg-info*
slurm-*
._*

# generated pipeline outputs and scratch data - reproducible via Snakemake, too large/volatile to track
data/raw/*
data/prep/*
results/*
.tmp/*
notebooks/.hbb_rendered
